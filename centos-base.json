{
    "builders": [
        {
            "type": "docker",
            "image": "eneset/centos7",
            "commit": true
        }
    ],
    "provisioners": [
        {
            "type": "ansible-local",
            "playbook_file": "playbooks/centos-base.yml",
            "playbook_dir": "playbooks"
        }
    ],
    "post-processors": [
        [
            {
                "type": "docker-tag",
                "repository": "eneset/centos-base",
                "tag": "latest"
            }
        ]
    ]
}
